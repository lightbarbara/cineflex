import styled from "styled-components";

export default function Footer() {
    return (
        <FooterContainer>
            
        </FooterContainer>
    )
}

const FooterContainer = styled.div`

`